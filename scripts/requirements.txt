# Requirements for scripts/ — requires Python 3.9-3.12 (lean-dojo incompatible with 3.13+)
# Setup: python3.12 -m venv scripts/.venv && scripts/.venv/bin/pip install -r scripts/requirements.txt
lean-dojo>=4.20.0
