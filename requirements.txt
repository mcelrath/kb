# kb runtime dependencies — enumerated from kb.py + kb/ package imports
# (ast-grep scan 2026-06-12; scripts/requirements.txt covers dev/ingest-only deps)

# Required: vector similarity extension for SQLite
sqlite-vec>=0.1.0

# Required: YAML config (kb/configure.py, kb/issue_cli.py)
pyyaml>=6.0

# Optional but included: tree-sitter code chunker (kb/code_ingest/chunker.py)
# Lazy-imported; kb works without these if you do not use `kb ingest python`
tree-sitter>=0.25.0
tree-sitter-rust>=0.24.0
tree-sitter-typescript>=0.23

# Optional: progress bars for `kb refresh` / `kb reembed`
# Lazy-imported; silently degrades to no-bar if absent
tqdm>=4.64.0

# Optional: rich terminal output for `kb get` (markdown rendering)
# Lazy-imported; falls back to plain text if absent
rich>=13.0.0

# Optional: web UI for `kb serve`
# Lazy-imported; `kb serve` prints an error and exits if absent
starlette>=0.27.0
uvicorn>=0.22.0

# NOT included (dev / ingest-only / personal-physics):
#   lean-dojo-v2, loguru, PyGithub  — scripts/requirements.txt, ingest_lean.py only
#   claude_agent_sdk                — subscription-sdk summary mode (KB_SUMMARY_MODE=subscription-sdk)
#   requests                        — NOT used; kb uses stdlib urllib throughout
